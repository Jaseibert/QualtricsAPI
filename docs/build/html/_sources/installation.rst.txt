Installation
=================================

Python Package Installer:
###########################

The installation of this package is pretty straight forward. Use the python package installer to install this package.
Run pip install in your terminal.
::
    $ pip install QualtricsAPI
